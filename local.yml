---
- hosts: compute
  user: root
  connection: local
  tasks:
    - yum:
        name="gnuplot"
        state=installed

- hosts: login
  user: root
  connection: local
  tasks:
    - yum:
        name="mysql"
        state=installed
